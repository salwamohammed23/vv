### **الانحدار الخطي البسيط (Simple Linear Regression) - شرح شامل مع أمثلة عملية**

#### **1. ما هو الانحدار الخطي البسيط؟**
هو نموذج إحصائي **لتحليل العلاقة بين متغيرين**:
- **متغير مستقل (X)** (المتنبئ)
- **متغير تابع (Y)** (النتيجة)

بحيث نبحث عن **علاقة خطية** بينهما تمثل بالمعادلة:
\[
Y = a + bX + e
\]
حيث:
- `a` = الجزء الثابت (Intercept)
- `b` = معامل الانحدار (Slope)
- `e` = الخطأ العشوائي

---

#### **2. متى نستخدمه؟**
- للتنبؤ بقيمة Y بناءً على X  
  (مثال: التنبؤ بالمبيعات بناءً على الإنفاق الإعلاني)
- لفهم قوة وتجاهل العلاقة بين المتغيرين

---

#### **3. خطوات تطبيق الانحدار الخطي**

**الخطوة 1: جمع البيانات**  
مثال:  
| الإنفاق الإعلاني (X) | المبيعات (Y) |
|----------------------|--------------|
| 100                  | 200          |
| 200                  | 300          |
| 300                  | 400          |
| 400                  | 500          |

**الخطوة 2: رسم مخطط الانتشار (Scatter Plot)**  
- للتأكد من وجود نمط خطي

**الخطوة 3: حساب معاملات النموذج**  
\[
b = \frac{n(\sum XY) - (\sum X)(\sum Y)}{n(\sum X^2) - (\sum X)^2}
\]
\[
a = \bar{Y} - b\bar{X}
\]

**الخطوة 4: تفسير النتائج**  
- **معامل التحديد (R²)**: نسبة التباين في Y المفسرة بواسطة X (من 0 إلى 1)
- **اختبار t**: لمعرفة إذا كان المعامل ذو دلالة إحصائية (p-value < 0.05)

---

#### **4. مثال تطبيقي**

**البيانات**:  
| X | Y |
|---|----|
| 1 | 2 |
| 2 | 3 |
| 3 | 5 |
| 4 | 4 |

**الحسابات**:  
1. احسب المجاميع:
   - \(\sum X = 10\)
   - \(\sum Y = 14\)
   - \(\sum XY = 1×2 + 2×3 + 3×5 + 4×4 = 33\)
   - \(\sum X^2 = 1 + 4 + 9 + 16 = 30\)

2. احسب الميل (b):
   \[
   b = \frac{4(33) - (10)(14)}{4(30) - (10)^2} = \frac{132 - 140}{120 - 100} = \frac{-8}{20} = -0.4
   \]

3. احسب الثابت (a):
   \[
   a = \bar{Y} - b\bar{X} = \frac{14}{4} - (-0.4)×\frac{10}{4} = 3.5 + 1 = 4.5
   \]

4. **معادلة الانحدار**:
   \[
   Y = 4.5 - 0.4X
   \]

---

#### **5. تفسير النتائج**
- **الميل (b = -0.4)**: كل زيادة وحدة في X تقلل Y بمقدار 0.4
- **الثابت (a = 4.5)**: قيمة Y عندما X = 0
- **R²**: لو كانت 0.8 يعني 80% من التباين في Y يُفسر بـ X

---

#### **6. شروط الاستخدام**
1. **العلاقة خطية** بين X و Y (تحقق بالمخطط البياني)
2. **تباين ثابت** للقيم (Homoscedasticity)
3. **الاستقلالية**: لا ترتبط ملاحظات X ببعضها
4. **التوزيع الطبيعي** للأخطاء (الفرق بين Y الفعلية والمتوقعة)

---

#### **7. مقارنة مع الارتباط (Correlation)**
| الانحدار | الارتباط |
|----------|----------|
| يبني نموذج للتنبؤ | يقيس قوة العلاقة فقط |
| له اتجاه (Y تعتمد على X) | لا يوجد متغير تابع |
| يعطي معادلة رياضية | يعطي رقم بين -1 و 1 |

---

#### **8. تمارين تطبيقية**
**التمرين 1**:  
إذا كانت معادلة الانحدار \( Y = 10 + 2X \):
- ما قيمة Y المتوقعة عندما X = 3؟
- إذا زاد X بمقدار 1، كم تتغير Y؟

**التمرين 2**:  
احسب معادلة الانحدار للبيانات:
| X | Y |
|---|----|
| 2 | 5 |
| 4 | 7 |
| 6 | 9 |

---

#### **9. استخدام Excel أو SPSS**
- **في Excel**:  
  `=SLOPE(Y_range, X_range)` للميل  
  `=INTERCEPT(Y_range, X_range)` للثابت  
  `=RSQ(Y_range, X_range)` لـ R²

- **في SPSS**:  
  Analyze > Regression > Linear

---

#### **10. تحذيرات شائعة**
- **لا تستخدمه للتنبؤ خارج نطاق البيانات** (Extrapolation)
- **الانحدار لا يعني السببية** (Correlation ≠ Causation)
- تأكد من عدم وجود **قيم متطرفة** (Outliers) تؤثر على النتائج

---

**المراجع**:  
- كتاب "Applied Linear Regression" لـ Sanford Weisberg  
- موقع Statistics By Jim للمفاهيم الأساسية
