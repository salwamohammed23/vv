شكرًا لتأكيدك. سأقوم الآن بـ:

✅ **ترجمة وتعريب كامل محتوى الملف** إلى اللغة العربية الفصحى
✅ **استبدال كل الكلمات الإنجليزية التقنية بمقابلها العربي**
✅ **تنسيقه بطريقة منظمة قابلة للحفظ كـ PDF أو Word**

---

### 📘 المعالجة الإحصائية باستخدام برنامج SPSS

#### 🔹 أولاً: حساب الوسط الحسابي (Mean)

**الخطوات الأساسية:**

1. **فتح برنامج SPSS.**

2. **إدخال البيانات:**
   انتقل إلى نافذة **عرض البيانات (Data View)**، وابدأ بإدخال القيم، وسمّ المتغيرات من خلال نافذة **عرض المتغيرات (Variable View)**، مثل: "العمر"، "الدرجات"، وغيرها. تأكد أن نوع المتغير "رقمي".

3. **الوصول إلى قائمة التحليل الإحصائي:**

   * اضغط على قائمة **تحليل (Analyze)**.
   * اختر **الإحصاءات الوصفية (Descriptive Statistics)**.
   * ثم اختر **الوصفية (Descriptives)**.

4. **اختيار المتغيرات:**

   * حدد المتغيرات التي تريد حساب الوسط الحسابي لها.
   * اضغط على زر **الخيارات (Options)**، وحدد **الوسط الحسابي (Mean)**.
   * اضغط على **متابعة (Continue)** ثم **موافق (OK)**.

5. **عرض النتائج:**
   ستظهر نافذة **المخرجات (Output Viewer)** وفيها جدول يحتوي على قيمة **الوسط الحسابي** لكل متغير تم اختياره.

---

#### 🔹 ثانيًا: حساب المنوال (Mode)

**خطوات التنفيذ:**

1. من قائمة **تحليل (Analyze)**:

   * اختر **الإحصاءات الوصفية (Descriptive Statistics)**.
   * ثم اختر **التكرارات (Frequencies)**.

2. حدد المتغير المطلوب ثم:

   * اضغط على زر **الإحصاءات (Statistics)**.
   * فعّل خيار **المنوال (Mode)**.
   * يمكنك كذلك تحديد **الوسيط (Median)** و**الوسط الحسابي (Mean)** إذا أردت.
   * اضغط على **متابعة (Continue)** ثم **موافق (OK)**.

3. سيظهر جدول يحتوي على **المنوال** ضمن نتائج المخرجات.

**تنبيه:**
إذا لم تكن بحاجة لجداول التكرارات، يُفضل إلغاء تفعيل خيار **عرض جداول التكرار (Display frequency tables)** لتقليل حجم النتائج.

---

#### 🔹 ثالثًا: حساب الانحراف المعياري (Standard Deviation)

**خطوات التنفيذ:**

1. من قائمة **تحليل (Analyze)**:

   * اختر **الإحصاءات الوصفية (Descriptive Statistics)**.
   * ثم اختر **الوصفية (Descriptives)**.

2. حدد المتغيرات ثم:

   * اضغط على زر **الخيارات (Options)**.
   * فعّل خيار **الانحراف المعياري (Standard Deviation)**.
   * اضغط على **متابعة (Continue)** ثم **موافق (OK)**.

3. سيظهر **الانحراف المعياري** لكل متغير في نافذة النتائج.

---

#### 📝 ملاحظات عامة:

* **الوصفية (Descriptives):** مناسبة لحساب الوسط والانحراف المعياري بسرعة.
* **التكرارات (Frequencies):** أفضل لحساب المنوال والوسيط وجداول التكرار.
* عند وجود أكثر من قيمة مكررة بشكل متساوٍ، قد يُظهر البرنامج أكثر من منوال.
* في حالة توزيع البيانات بشكل غير طبيعي، يُفضل استخدام **الوسيط** بدلاً من **الوسط الحسابي**.

---

هل ترغبين الآن أن أحوّل هذا المحتوى إلى **ملف Word أو PDF** جاهز للتحميل؟
